<%# Movies Index - Showcases: IndexPage, DataTable, Filters, Table, Tag, BooleanIcon, ActionsDropdown, Link %>

<%= render Bali::IndexPage::Component.new(
  title: t('movies.title'),
  subtitle: t('movies.subtitle'),
  breadcrumbs: [
    { name: t('navbar.dashboard'), href: root_path, icon_name: 'home' },
    { name: t('movies.title') }
  ]
) do |page| %>
  <% page.with_action do %>
    <%= render Bali::Link::Component.new(
      name: t('movies.new_movie'),
      href: new_movie_path,
      variant: :primary,
      icon_name: 'plus'
    ) %>
  <% end %>

  <%# El DataTable entra pelado: la superficie viaja con su slot de contenido. %>
  <% page.with_body do %>
    <%= render 'listing' %>
  <% end %>
<% end %>
