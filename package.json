{
  "name": "bali-view-components",
  "version": "0.0.1",
  "description": "Bali ViewComponents",
  "main": "app/javascript/bali/index.js",
  "repository": "git@github.com:Grupo-AFAL/bali.git",
  "author": "Federico Gonzalez <fedegl@hey.com>",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@hotwired/stimulus": "^3.0.1"
  },
  "scripts": {
    "cy:open": "yarn install && yarn --cwd spec/dummy/ build && cypress open",
    "cy:run": "yarn install && yarn --cwd spec/dummy/ build && cypress run"
  },
  "devDependencies": {
    "cypress": "^9.6.1"
  }
}
