<li>
  <hr class="<%= line_classes %>"/>
  <div class="<%= content_box_classes %>">
    <% if heading.present? %>
      <p class="font-semibold"><%= heading %></p>
    <% end %>
    <%= content %>
  </div>
  <div class="<%= marker_classes %>">
    <%= render Bali::Icon::Component.new(display_icon, size: :small) %>
  </div>
  <hr class="<%= line_classes %>"/>
</li>
