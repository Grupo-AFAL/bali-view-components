<div id="modal-template" class="<%= modal_classes %> fixed inset-0 z-50 items-center justify-center hidden [&.modal-open]:flex" data-modal-target="template">
  <div class="modal-backdrop fixed inset-0 bg-black/50" data-modal-target="background" data-action="click->modal#close"></div>
  <div class="<%= box_classes %> relative bg-base-100 rounded-box p-6 shadow-xl w-11/12 max-h-[calc(100vh-5em)] overflow-y-auto" data-wrapper-class="<%= @wrapper_class %>" data-modal-target="wrapper">
    <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2" aria-label="close" data-action="modal#close">✕</button>
    <div class="modal-inner" data-modal-target="content">
      <%= content %>
    </div>
  </div>
</div>
