<%= tag.div(id: input_id) do %>
  <label class="label">
    <%= @title %>
    <span class="is-size-7 has-text-grey"><%= t("ransack.predicates.#{@predicate}") %></span>
  </label>

  <div class="control">
    <%= number_field_tag input_name, @form.send(@attribute), class: 'input' %>
  </div>
<% end %>
