<%= form_with(url: helpers.request.path, local: true) do |form| %>
  <%= render(Bali::Avatar::Upload::Component.new(
    form: form,
    method: :avatar,
    src: 'avatar.png',
    size: size,
    shape: shape
  )) %>
<% end %>
