<%= form_with(url: '/') do |form| %>
  <%= render(Bali::Avatar::Component.new(form: form, attr_model_name: :avatar, 
                                         accepted_formats: '.jpg, .jpeg, .png')) do |c| %>

    <% c.picture(picture: image_url) %>
  <% end %>
<% end %>
