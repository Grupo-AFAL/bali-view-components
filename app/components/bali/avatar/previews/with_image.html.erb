<%= form_with(url: '/') do |form| %>
  <%= render(Bali::Avatar::Component.new(form: form, method: :avatar, placeholder_url: 'placeholder-avatar.png')) do |c| %>
    <% c.with_picture(image_url: image_url) %>
  <% end %>
<% end %>
