<%= form_with(model: model, url: '/lookbook', builder: Bali::FormBuilder) do |form| %>
  <div class="flex flex-wrap gap-4">
    <%= form.submit_field 'Primary', variant: :primary %>
    <%= form.submit_field 'Secondary', variant: :secondary %>
    <%= form.submit_field 'Accent', variant: :accent %>
    <%= form.submit_field 'Success', variant: :success %>
    <%= form.submit_field 'Warning', variant: :warning %>
    <%= form.submit_field 'Error', variant: :error %>
    <%= form.submit_field 'Ghost', variant: :ghost %>
  </div>
<% end %>
