<%= render Bali::Alert::Component.new(title: title, color: :error, **component_options) do %>
  <ul class="list-disc list-inside">
    <% error_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
