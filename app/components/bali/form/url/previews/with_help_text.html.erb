<%= form_with(model: model, url: '/', builder: Bali::FormBuilder) do |form| %>
  <%= form.url_field_group :url,
      placeholder: 'https://example.com',
      help: 'Enter a valid URL including the protocol (https://).' %>
<% end %>
