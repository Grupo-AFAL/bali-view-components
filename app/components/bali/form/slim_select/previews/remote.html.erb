<%= form_with(model: model, url: '/', builder: Bali::FormBuilder) do |form| %>
  <%= form.slim_select_field :name, options, {
        ajax_param_name: 'q[name_i_cont]',
        ajax_value_name: 'id',
        ajax_text_name: 'full_name',
        ajax_url: '/users.json',
        ajax_placeholder: 'Start typing to get results...'
      } %>
<% end %>
