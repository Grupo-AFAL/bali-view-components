<%= tag.div **@options do %>
  <%= tag.div class: class_names('item', 'is-childless': !items?, 'is-root': @root, 'is-active': active?) do %>
    <%= tag.span(class: class_names('caret', 'caret-down': display_children?),
                 data: { action: 'click->tree-view-item#toggle', 'tree-view-item-target': 'caret' }) %>
    <%= link_to @name, @path, title: @name %>
  <% end %>

  <% if items? %>
    <%= tag.div(
        class: class_names('children', 'hidden': !display_children?),
        data: { 'tree-view-item-target': 'children' }
    ) do %>
      <% items.each do |item| %>
        <%= item %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
