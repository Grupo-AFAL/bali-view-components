<%= tag.div **@options do %>
  <div class="card">
    <div class="card-image">
      <figure class="image <%= image_ratio %> [&_img]:w-full [&_img]:h-full">
        <%= content %>
      </figure>
    </div>

    <% if footer? %>
      <%= footer %>
    <% end %>
  </div>
<% end %>
