<div class="flex flex-col gap-4 w-full max-w-md">
  <% Bali::Progress::Component::COLORS.keys.each do |color| %>
    <div class="flex items-center gap-4">
      <span class="w-20 text-sm"><%= color %></span>
      <%= render Bali::Progress::Component.new(value: 70, color: color, show_percentage: false, class: 'flex-1') %>
    </div>
  <% end %>
</div>
