name: Cypress Tests
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cypress:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Setup database
        run: cd spec/dummy && bin/rails db:schema:load RAILS_ENV=test

      - name: Install dependencies
        run: |
          yarn install
          cd spec/dummy && yarn install

      - name: Build Tailwind CSS
        run: cd spec/dummy && bin/rails tailwindcss:build

      - name: Build JS assets
        run: cd spec/dummy && yarn build

      - name: Precompile assets
        run: cd spec/dummy && bin/rails assets:precompile

      - name: Start server
        run: cd spec/dummy && bin/rails server -p 3001 &

      - name: Wait for server
        run: npx wait-on http://127.0.0.1:3001 --timeout 60000

      - name: Run Cypress tests
        run: yarn cypress run --browser chrome
